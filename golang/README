1. 编译
   依赖librdkafka，先执行：
   kafkabridge/cxx/build_librdkafka.sh 
   再执行./build.sh

2.在当前目当下将产生gopath/src/qbus目录，里面有qbus.go和libQBus_go.so文件


3. 清理编译
   make clean

5. 例子代码在exmaples中，编译请使用build.sh脚本

